using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerEquipmentManager : MonoBehaviour
{
    ////設定された装備値を参照してScriptableのデータを呼び出す。
    //装備値。
    public int EquipInt;

    void Start()
    {
        
    }

    
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.F1))
        {

        }

        if(Input.GetButtonDown("Fire1"))
        {

        }
    }
}
